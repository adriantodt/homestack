# Exposed ports
PORT_PORTAINER_MAIN=8081
PORT_FILEBROWSER_MAIN=8082
PORT_QBITTORRENT_MAIN=8083

# Root directory of stack, must not end with a "/"
STACK_ROOT="./run"

# Linux User and Group to own the files
# Set 0 for all for "root" user
# Run `id $(whoami)` on bash / `id (whoami)` on fish
FILE_OWNER_PUID=1000
FILE_OWNER_PGID=1000

# LinuxServer Timezone
LINUXSERVER_TZ="Europe/London"